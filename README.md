# Crawler/Parser Backend Service

## 📋 Описание проекта

Бэкенд-сервис для парсинга публичных веб-страниц с очередью задач. Сервис позволяет:
- Отправлять список URL для парсинга через HTTP API
- Обрабатывать задачи асинхронно через очередь
- Извлекать структурированные данные (профили) из HTML
- Отслеживать статус выполнения задач
- Искать сохраненные профили

**Важные ограничения:**
- Парсинг только публичных страниц
- Без обхода защитных механизмов
- Соблюдение rate limits (1 запрос/сек на домен)

## 🏗️ Архитектура

### Компоненты системы:

```
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│  HTTP API   │────▶│    Queue     │────▶│   Worker    │
│  (Express)  │     │   (BullMQ)   │     │             │
└─────────────┘     └──────────────┘     └─────────────┘
       │                    │                     │
       ▼                    ▼                     ▼
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│ PostgreSQL  │     │    Redis     │     │   Parser    │
│  (Prisma)   │     │              │     │ (Cheerio/   │
└─────────────┘     └──────────────┘     │ Playwright) │
                                         └─────────────┘
```

### Основные технологии:

- **HTTP API**: Express.js + TypeScript
- **Queue**: BullMQ + Redis
- **Worker**: Обработка задач с concurrency control
- **Parser**: 
  - HTTP fetcher + Cheerio (основной)
  - Playwright (fallback для JS-рендеринга)
- **База данных**: PostgreSQL + Prisma ORM

## 📊 Job Flow

1. **POST /api/crawl** - клиент отправляет список URL
2. **Job создается** в БД со статусом `queued`
3. **JobItems создаются** для каждого URL
4. **Job попадает в очередь** BullMQ
5. **Worker забирает Job** и обрабатывает items параллельно
6. **Для каждого item**:
   - Fetch с retry (max 3 попытки)
   - Parse HTML → DTO
   - Save в БД или update статус
7. **Job статус обновляется**: `done`/`partial`/`failed`
8. **GET /api/jobs/:id** - клиент проверяет результат

## 🔄 Парсинг и деградация

### Основной путь (HTTP + Cheerio):
- Быстрый HTTP-запрос
- Парсинг HTML через Cheerio
- Извлечение данных по CSS-селекторам

### Fallback (Playwright):
- Активируется при пустом результате от Cheerio
- Полный рендеринг страницы в браузере
- Ожидание загрузки JS-контента

### Обработка ошибок:
- `403 Forbidden` → статус `blocked` (без retry)
- `429/5xx` → retry с exponential backoff
- Network errors → retry
- Остальное → `error`

## 🔁 Retry & Rate Limit

### Retry политика:
- **MAX_ATTEMPTS**: 3
- **BACKOFF_BASE_MS**: 500ms
- **Backoff**: exponential (500ms, 1s, 2s)
- **Retry для**: Network errors, 429, 5xx
- **Без retry для**: 403, 4xx (кроме 429)

### Rate limiting:
- **1 запрос/секунда** на домен
- In-memory хранение timestamps
- Автоматическая задержка между запросами

## 📡 API Reference

### POST /api/crawl
Создает задачу парсинга для списка URL.

```bash
curl -X POST http://localhost:3000/api/crawl \
  -H "Content-Type: application/json" \
  -d '{
    "urls": ["https://example.com"],
    "priority": "normal"
  }'
```

Response:
```json
{
  "jobId": "01J..."
}
```

### GET /api/jobs/:jobId
Получает информацию о задаче.

```bash
curl http://localhost:3000/api/jobs/01J...
```

Response:
```json
{
  "job": {
    "id": "01J...",
    "status": "done",
    "priority": "normal",
    "createdAt": "2024-01-01T00:00:00Z",
    "updatedAt": "2024-01-01T00:01:00Z",
    "completedAt": "2024-01-01T00:01:00Z",
    "items": [
      {
        "id": "01J...",
        "sourceUrl": "https://example.com",
        "status": "ok",
        "attempts": 1,
        "profileId": "01J..."
      }
    ]
  }
}
```

### GET /api/profiles
Поиск сохраненных профилей.

```bash
# Все профили с пагинацией
curl "http://localhost:3000/api/profiles?page=1&limit=20"

# Поиск по тексту
curl "http://localhost:3000/api/profiles?query=username&page=1&limit=20"
```

Response:
```json
{
  "page": 1,
  "limit": 20,
  "total": 100,
  "items": [
    {
      "sourceUrl": "https://example.com/user",
      "username": "username",
      "displayName": "Display Name",
      "avatarUrl": "https://..."
    }
  ]
}
```

### GET /api/health
Проверка состояния сервиса.

```bash
curl http://localhost:3000/api/health
```

## 🚀 Как запустить проект

### Предварительные требования:
- Node.js 18+
- Docker & Docker Compose
- npm или yarn

### Шаги установки:

1. **Клонировать репозиторий и установить зависимости:**
```bash
git clone <repository>
cd parser_testing
npm install
```

2. **Настроить environment:**
```bash
cp .env.example .env
```

3. **Запустить инфраструктуру (PostgreSQL + Redis):**
```bash
docker-compose up -d
```

4. **Применить миграции БД:**
```bash
npx prisma migrate dev
```

5. **Запустить API сервер:**
```bash
npm run dev
```

6. **Запустить Worker (в отдельном терминале):**
```bash
npm run worker
```

### Проверка работоспособности:

1. **Health check:**
```bash
curl http://localhost:3000/api/health
```

2. **Создать задачу парсинга:**
```bash
curl -X POST http://localhost:3000/api/crawl \
  -H "Content-Type: application/json" \
  -d '{"urls": ["https://example.com"], "priority": "normal"}'
```

3. **Проверить статус задачи:**
```bash
curl http://localhost:3000/api/jobs/<jobId>
```

## 🧪 Тесты

### Запуск тестов:
```bash
npm test
```

### Типы тестов:

1. **Unit тесты**:
   - Парсинг HTML → DTO (`tests/parser/cheerio-parser.test.ts`)
   - Playwright utilities (`tests/parser/playwright-utils.test.ts`)

2. **Integration тесты**:
   - Job flow (`tests/integration/job-flow.test.ts`)
   - Сервисы и репозитории

3. **E2E тесты**:
   - HTTP API endpoints (`tests/e2e/api.test.ts`)
   - Валидация запросов
   - Форматы ответов

### Coverage:
```bash
Test Suites: 4 passed, 4 total
Tests:       33 passed, 33 total
```

## 📝 Ограничения и возможные улучшения

### Текущие ограничения:
- Нет поддержки прокси
- Нет решения captcha
- Rate limiter хранит данные in-memory (не масштабируется)
- Нет авторизации/аутентификации API
- Ограниченный набор парсеров (только OnlyFans-like)

### Возможные улучшения (out of scope):
- Распределенный rate limiter через Redis
- Поддержка proxy rotation
- WebSocket для real-time обновлений статуса
- Метрики и мониторинг (Prometheus/Grafana)
- Горизонтальное масштабирование workers
- Поддержка других типов страниц/платформ

## ✅ Соответствие техническому заданию

- [x] **Express.js + TypeScript** - REST API на Express с полной типизацией
- [x] **Очередь задач** - BullMQ для асинхронной обработки
- [x] **Concurrency control** - настраиваемый параллелизм в worker
- [x] **Rate limit** - 1 req/sec на домен
- [x] **Retries** - exponential backoff, max 3 попытки
- [x] **PostgreSQL + Prisma** - структурированное хранение данных
- [x] **Парсинг публичных страниц** - без обхода защит
- [x] **Playwright fallback** - для JS-rendered страниц
- [x] **API endpoints** - `/crawl`, `/jobs`, `/profiles`
- [x] **Unit / Integration / E2E tests** - полное покрытие
- [x] **Graceful degradation** - обработка всех типов ошибок

## 📦 Структура проекта

```
parser_testing/
├── src/
│   ├── api/          # HTTP API (controllers, routes, middleware)
│   ├── db/           # Prisma схема и репозитории
│   ├── parser/       # Парсинг логика (HTTP, Playwright, parsers)
│   ├── queue/        # BullMQ конфигурация
│   ├── services/     # Бизнес-логика
│   ├── utils/        # Утилиты (retry, rate-limiter, logger)
│   └── workers/      # Queue workers
├── tests/            # Тесты
├── prisma/           # Миграции БД
├── docker-compose.yml
├── package.json
├── tsconfig.json
└── README.md
```